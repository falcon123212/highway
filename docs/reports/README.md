# Reports

Canonical validating reports are stored under `artifacts/runs/` and referenced here for tracking validation gates.

## Reference Runs

- **Deterministic Kernel Hardening & No-Leak Validation**:
  - `artifacts/runs/poc_2_3_4_no_leak/`
- **RAGBench Mini Stress (Sentence Packing & Aggregation Sweeps)**:
  - `artifacts/runs/ragbench_ministress_poc_16_6_2_fullmini/`

Historical reports remain in their original run directories under `artifacts/runs/` unless a later cleanup phase archives them explicitly.

Root-level legacy artifacts are archived under:

- `docs/reports/historical/`


